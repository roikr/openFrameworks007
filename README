things to do (ihpone->osx):
1. copy all iPhone addons + apps
2. copy all libs (openframeworks compiled + project, freetype and freeimage libs)
3. overwrite ofConstants.h, ofTexture.cpp, ofBitmapFont.h+cpp, ofGraphics.cpp

iPhone ofxOsc
1. you need to find the cocoaoscpack project and get the oscpack
2. create new iPhone library project - Osc add it to the oscpack folder from cocoaoscpack
3. set the header folders
4. compile iPhone libOsc library and overite ofxOsc's oscpack includes+lib (for iPhone)

